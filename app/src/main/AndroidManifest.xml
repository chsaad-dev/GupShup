<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.example.gupshup">

    <!-- ✅ Permissions -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <!-- Optional for older devices (Android < Q) -->
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE"
        android:maxSdkVersion="28" />

    <application
        android:name=".GupShupApp"
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.GupShup"
        tools:targetApi="31">

        <!-- ✅ Authentication -->
        <activity android:name=".ui.auth.RegisterActivity" />
        <activity android:name=".ui.auth.LoginActivity" />

        <!-- ✅ Splash Screen -->
        <activity
            android:name=".SplashActivity"
            android:exported="true"
            android:theme="@style/Theme.GupShup.Splash">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".ui.auth.EmailVerificationActivity"/>

        <!-- ✅ Chat Screens -->
        <activity android:name=".ui.chat.ChatActivity" />
        <activity android:name=".MainActivity" android:exported="false" />
        <activity android:name=".ui.chat.StatusViewActivity" />
        <activity android:name=".ui.chat.StatusStoryActivity"/>



        <!-- ✅ Main Navigation Screen -->
        <activity
            android:name=".ui.main.MainNavigationActivity"
            android:windowSoftInputMode="adjustPan"
            android:exported="false"
            android:theme="@style/Theme.GupShup" />

        <!-- ✅ Settings Screen -->
        <activity
            android:name=".ui.main.SettingsActivity"
            android:exported="false"
            android:theme="@style/Theme.GupShup" />

    </application>

</manifest>

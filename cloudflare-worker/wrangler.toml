name = "gupshup-notifications"
main = "src/index.ts"
compatibility_date = "2024-05-29"

[vars]
PROJECT_ID = "gupshup-69749"

# Secrets required (configured via 'wrangler secret put' or .dev.vars for local dev):
# FIREBASE_CLIENT_EMAIL = "..."
# FIREBASE_PRIVATE_KEY = "..."
